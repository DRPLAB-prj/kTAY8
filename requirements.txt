# Core dependencies for kTAY8 Server
flask
waitress
Pillow
numpy
opencv-python
requests
matplotlib

# AI / Inference Dependencies
# For ONNX support (Recommended fallback)
onnxruntime

# For TFLite support (Recommended for Orange Pi / ARM)
# Note: On some ARM systems (like Orange Pi), it is better to install tflite-runtime 
# via system packages or specific wheels to avoid building from source.
# tflite-runtime
